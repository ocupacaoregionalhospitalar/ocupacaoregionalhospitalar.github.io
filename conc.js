
function conc() 
{
  background (background_gradient);
  
  let title_size = 20;
  textSize (title_size);
  textAlign (CENTER, CENTER);
  fill (0);
  
  textSize (title_size);
  textStyle(BOLD);
  textAlign (CENTER, CENTER);
  fill (255);
  
  text ("conclusão.......", width/2, 200 + width/15, width/1.2, height - title_size*2);
}

